name: backend_cd

on:
  workflow_dispatch:

env:
  env_file: ${{ secrets.env_prod }}
  aws_pem_file: ${{ secrets.aws_pem_file }}
  docker_password: ${{ secrets.docker_password }}
jobs:
  Testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main 
      - run: ls && echo "$env_file" > ".env" && touch test.txt && echo "$aws_pem_file" > aws.pem && chmod 600 aws.pem && ls && scp -i aws.pem -o StrictHostKeyChecking=no test.txt ec2-user@35.183.197.0:/home/ec2-user
      
      #docker login --username swanso15 --password $docker_password && docker build -t swanso15/cupboard_backend:latest . && docker image ls && docker save -o cupboard_backend.tar swanso15/cupboard_backend:latest
